// Guardar la posición del jugador
var _pos_x = other.x;
var _pos_y = other.y;

// Destruir la instancia del jugador
if (!transformed){
	transformed = !transformed
	with (other) {
		transformation = !transformation
	    //instance_destroy();
		
	}

	// Crear la nueva instancia del jugador transformado
	//instance_create_layer(_pos_x, _pos_y, "Instances", obj_player_flyer);

}